@model Quiz.Models.University
    <div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <input type="submit" class="btn btn-default" value="Add" />
            </div>
        }
    </div>